// Fill these in when available; the values are applied to the markup at runtime.
// Waitlist form link (applied to every [data-waitlist] button/link):
export const WAITLIST_URL = '' // e.g. 'https://tally.so/r/xxxxx'

// Contact (the CTA + footer currently show placeholders hello@flaneur.app / @flaneur).
// When you have the real ones, set them here and they'll override the placeholders.
export const CONTACT_EMAIL = '' // e.g. 'you@domain.com'
export const INSTAGRAM_URL = '' // e.g. 'https://instagram.com/flaneur.app'
